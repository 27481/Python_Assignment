# Write a python script to check if a string contains only numbers.

strr=input("Enter the input string to check :\n")

if(strr.isdigit()):
    print("String contains only numbers :\n")
else:
    print("String does not contain only number it contains a mixture")